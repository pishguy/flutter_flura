name: todo_app
description: Flura framework example — local todo app with Capsa reactive UI.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flura:
    path: ../../packages/flura
  capsa:
    path: /Users/mahdipishguy/Desktop/projects/flutter/capsa

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
